emails = []
jobs = []
while True:
  # Read unread emails into emails
  # for i in emails:
    # if i.subject[:18] == 'TradingView Alert:':
      # command = i.text
  if *keyboard_input: space* == True:
    break
